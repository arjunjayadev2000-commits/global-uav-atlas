"""PyQt6 application for the multi-drone SDR tracker.

Run it with ``python -m sdr.gui.app``. Importing this package pulls in
PyQt6, so the headless tracker in :mod:`sdr.multi_drone_tracker` does not
import it -- the processing core stays usable with no GUI stack installed.
"""
